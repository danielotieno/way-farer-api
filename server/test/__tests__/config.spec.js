describe('config Environments ', () => {
  describe('Get env variables', () => {
    test('it should get env', () => {
      // lets test it can define true
      expect(true).toEqual(true)
    })
    test('it should get port', () => {
      // lets test it can define true
      expect(true).toEqual(true)
    })
  })
})
